/**
 * Configuration constants for the C# Debug Hints extension
 */

// Polling interval for checking debug state (in milliseconds)
export const POLL_INTERVAL_MS = 100;

// Maximum recursion depth when serializing objects
export const MAX_OBJECT_DEPTH = 5;

// Maximum display length for inline hint values
export const MAX_INLINE_VALUE_LENGTH = 100;

// Truncation suffix for long values
export const VALUE_TRUNCATE_SUFFIX = '...';
