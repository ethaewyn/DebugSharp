// Polling configuration
export const POLL_INTERVAL_MS = 100;

// Decoration styling
export const DECORATION_CONFIG = {
  after: {
    margin: '0 0 0 1rem',
    color: '#00FF00',
  },
};

// Value display settings
export const MAX_VALUE_LENGTH = 50;
export const VALUE_TRUNCATE_LENGTH = 47;
